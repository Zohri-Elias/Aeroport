{% extends 'base.html.twig' %}

{% block title %}Hello VolController!{% endblock %}
{% block content %}
    <h1>Ajouter un vol</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Ajouter</button>
    {{ form_end(form) }}
{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! ✅</h1>
        <p>{{ vol.id }}</p>
        <p>{{ vol.villeDepart }}</p>*
        <p>{{ vol.villeArrive }}</p>
        <p>{{ vol.dateDepart | date("d/m/Y")}}</p>
        <p>{{ vol.heureDepart | date("H:i")}}</p>
        <p>{{ vol.prixBilletInitiale }}</p>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code>C:/wamp64/www/Aeroport/src/Controller/VolController.php</code></li>
            <li>Your template at <code>C:/wamp64/www/Aeroport/templates/vol/index.html.twig</code></li>
        </ul>
    </div>
{% endblock %}
